import React from 'react';
import { View, Text, Button, StyleSheet } from 'react-native';

const Kep = ({ route, navigation }) => {
  

  return (
    <View style={styles.container}>
      

    </View>
  );
};

const styles = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: 'center',
    justifyContent: 'center',
  },
});

export default Kep;
